#include <stdio.h>
#include <string.h>


bool isExists(char table[][6], char* word) {
/*
	int mark[255];

	const int size = strlen(word);
	int row, column, index, markIndex = 0;

	for (index = 0; index < size; index++) {
		for (row = 0; row < 5; row++) {
			for (column = 0; column < 5; column++) {
				if (table[row][column] == word[index]) {
					mark[markIndex] = (row * 5) + column;
					markIndex++;
				}
			}
		}
	}

	int prevMark = -1;
	for (index = 0; index < size; index++) {		
		int currentMark = mark[index];
		if (prevMark > 0) {			
		}
		prevMark = currentMark;
		printf("%d\n", mark[index]);
	}	
*/
	return false;
}

int main(void) {

	int tc, T;
		
	freopen("input.txt", "r", stdin);
	
	setbuf(stdout, NULL);

	scanf("%d", &T);
	for(tc = 0; tc < T; tc++)
	{
		
		/**********************************
		*  Implement your algorithm here. *
		***********************************/		
		int row, column, size = 5;
		char table[5][6];
		
		for (row = 0; row < size; row++) {
			scanf("%s", table[row]);
		}

		int count, totalWords;
		scanf("%d", &totalWords);
		for (count = 0; count < totalWords; count++) {

			char word[255];

			scanf("%s", word);
			if (isExists(table, word)) {
				printf("%s YES\n", word);
			}
			else {
				printf("%s NO\n", word);
			}
		}
	}	
	
	return 0;
}


/*********************************

1
URLPM
XPRET
GIAET
XTNZY
XOQRS
6
PRETTY
GIRL
REPEAT
KARA
PANDORA
GIAZAPX

***********************************/
